{
  "version": 2,
  "installCommand": "yarn install",
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(.*\\.(svg|png|jpg|jpeg|ico|webp|woff2?|ttf|map|zip))", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
